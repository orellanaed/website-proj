import React from "react";
import { GatsbyImage, getImage } from "gatsby-plugin-image";

const BlockSlider = ({ data }) => {
  return <div className="container max-w-3xl py-8"></div>;
};

export default BlockSlider;
